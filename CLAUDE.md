# Project Guidelines

## Package Management

After installing, updating, or upgrading any npm package, always run `npm install` in the relevant frontend directory to ensure `package-lock.json` stays in sync.

```bash
cd escoresheet/frontend && npm install
```
